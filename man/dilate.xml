<?xml version="1.0" encoding="ISO-8859-1" standalone="no"?>
<!DOCTYPE MAN SYSTEM "mansivp.dtd">
<MAN>
<LANGUAGE>eng</LANGUAGE>
<TITLE>dilate</TITLE>
<TYPE>SIVP Toolbox</TYPE>
<DATE>August, 2005</DATE>
<SHORT_DESCRIPTION name="dilate">Dilates image by using arbitrary structuring element</SHORT_DESCRIPTION>
<CALLING_SEQUENCE>
   <CALLING_SEQUENCE_ITEM>ima_out = dilate(ima_in,param1,"shape",param2,param3,param4,param5) or by default dilate(ima_in,param1);</CALLING_SEQUENCE_ITEM>
</CALLING_SEQUENCE>
<PARAM>
<PARAM_INDENT>
   <PARAM_ITEM>
   <PARAM_NAME>ima_in</PARAM_NAME>
   <PARAM_DESCRIPTION>
   Input matrice.
   </PARAM_DESCRIPTION>
   </PARAM_ITEM>
   <PARAM_ITEM>
   <PARAM_NAME>ima_out</PARAM_NAME>
   <PARAM_DESCRIPTION>
   Output matrice.
   </PARAM_DESCRIPTION>
   </PARAM_ITEM>
   <PARAM_ITEM>
   <PARAM_NAME>param1</PARAM_NAME>
   <PARAM_DESCRIPTION>
   Number of times dilatation is applied.
   </PARAM_DESCRIPTION>
   </PARAM_ITEM>

   <PARAM_ITEM>
   <PARAM_NAME>"shape"</PARAM_NAME>
   <PARAM_DESCRIPTION>
   Optional argument : Shape of the structuring element; may have the following values:
   SHAPE_RECT , a rectangular element; 
   SHAPE_CROSS , a cross-shaped element; 
   SHAPE_ELLIPSE , an elliptic element;
   </PARAM_DESCRIPTION>
   </PARAM_ITEM>
 
   <PARAM_ITEM>
   <PARAM_NAME>"param2"</PARAM_NAME>
   <PARAM_DESCRIPTION>
   Optional argument : Creates structuring element.
   Number of columns in the structuring element.
   </PARAM_DESCRIPTION>
   </PARAM_ITEM>  

   <PARAM_ITEM>
   <PARAM_NAME>"param3"</PARAM_NAME>
   <PARAM_DESCRIPTION>
   Optional argument : Creates structuring element.
   Number of rows in the structuring element.
   </PARAM_DESCRIPTION>
   </PARAM_ITEM> 

   <PARAM_ITEM>
   <PARAM_NAME>"param4"</PARAM_NAME>
   <PARAM_DESCRIPTION>
   Optional argument : Creates structuring element.
   Relative horizontal offset of the anchor point.
   </PARAM_DESCRIPTION>
   </PARAM_ITEM> 
   
   <PARAM_ITEM>
   <PARAM_NAME>"param5"</PARAM_NAME>
   <PARAM_DESCRIPTION>
   Optional argument : Creates structuring element.
   Relative vertical offset of the anchor point.
   </PARAM_DESCRIPTION>
   </PARAM_ITEM> 
         
</PARAM_INDENT>
</PARAM>
<DESCRIPTION>
<P>
   <TT>dilate</TT>The function dilates the source image using a specified structuring element (B) that determines the shape of a pixel neighborhood over which the maximum is taken. by default it is a 3*3 rectangle.
</P>

<P>
C=dilate(A,B):  C(x,y)=max((x',y') in B(x,y))A(x',y')
</P>

<P>
The function supports the in-place mode when the source and destination pointers are the same. Dilation can be applied several times iterations parameter. Dilation on a color image means independent transformation of all the channels.
</P>
   
</DESCRIPTION>
<EXAMPLE><![CDATA[

//upgrade the size of the memory
stacksize(10000000); 

//load the picture
B=imread('/home/vincent/software/sivp/images/lena.tif');

//call the SIVP function
A=dilate(B,4);
//A=dilate(B,3,'RECT',3,3,1,1);
//A=dilate(C,4,'CROSS',3,3,1,1);
//A=dilate(B,5,'ELLIPSE',3,3,1,1);

//conversion
A=double(A)/255;

//try to display the result
imshow(A);

]]></EXAMPLE>
<!-- ================================= -->

<AUTHORS>
   <AUTHORS_ITEM name="ETIENNE"> ETIENNE Vincent &lt;vincent.etienne (AT) gmail DOT com &gt; </AUTHORS_ITEM>
</AUTHORS>
<!-- ================================= -->
<SECTION label="Bugs and Shortcomings">
<P>
</P>
</SECTION>
<!-- ================================= -->
<SECTION label="Availability">
The latest version of SIVP can be found at
<P><A href="http://sivp.sourceforge.net">http://sivp.sourceforge.net</A></P>
</SECTION>
<!-- ================================= -->
   <SEE_ALSO>
      <SEE_ALSO_ITEM> <LINK>erode</LINK> </SEE_ALSO_ITEM>
   </SEE_ALSO>
</MAN>
